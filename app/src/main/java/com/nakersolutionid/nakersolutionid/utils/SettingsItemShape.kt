package com.nakersolutionid.nakersolutionid.utils

enum class SettingsItemShape {
    TOP,      // The first item, with top corners rounded
    MIDDLE,   // A middle item, with no corners rounded (a rectangle)
    BOTTOM,   // The last item, with bottom corners rounded
    ALL      // The only item, with all four corners rounded
}